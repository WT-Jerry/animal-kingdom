# 動物王國語音導覽 · 設計總結

本檔整理主頁與二十六個子頁的定版，以及這段對話裡手機分區橫幅的定稿。  
公開站：https://wt-jerry.github.io/animal-kingdom/  
Repo：`git@github.com:WT-Jerry/animal-kingdom.git`（SSH 推 `main`）

---

## 一、這段對話在做什麼

主題是**主頁六大生態區的手機頂部橫幅**。作法統一：

1. Grok Imagine 生 **21:9**（必要時 **5:2**）靜態圖。
2. 去掉黑邊 letterbox。
3. **裁切**成手機扁框比例（約 **5.23:1**，對 390 寬螢幕約 **356×68**），**禁止**把較高的圖拉成扁框（會左右變形）。
4. 檔案放 `media/<zone>-sky.jpg`，手機 CSS 用 `background-size: cover`，**不要**用 `<img>`。
5. 先截圖給看，**說 OK 再推 GitHub**。桌面版維持漸層，不套這張橫幅。

| 分區 | 檔案 | 定稿畫面 | 曾否決 |
| --- | --- | --- | --- |
| 森林 | `media/forest-sky.jpg` | 原始雨林樹冠、藤蔓 | 泥土小徑、人工物件 |
| 草原 | `media/grass-sky.jpg?v=7` | 平視黃昏非洲大草原、金合歡剪影 | 俯視、翠綠俯視草皮、把圖拉寬 |
| 極地 | `media/polar-sky.jpg?v=1` | 平視藍天＋冰川 | 車痕、滑雪軌、腳印、展場人潮 |
| 沙漠 | `media/desert-sky.jpg?v=4` | 明顯沙丘起伏＋背光陰影 | 烈日圓盤（先要後拿掉） |
| 海洋 | `media/ocean-sky.jpg?v=2` | 開闊海面、浪花、地平線 | 清澈海水＋珊瑚礁（後來說遺忘） |
| 史前 | `media/pre-sky.jpg?v=1` | 白堊紀荒野、暴龍、翼龍 | 博物館、觀眾、圍繩 |

沙漠、海洋都經歷「先指定畫面 → 再改主題 → 定稿 OK」。之後若說「遺忘掉／重新建構符合這個主題」，以**最新一句**為準，不要沿用上一輪 prompt。

橫幅以外、這專案已定版且不要改回去的重點：

- 主頁**沒有**底部常駐播放器（`app.js` 已刪）。
- 主頁 footer **沒有**「夜裡走進王國，聲音會帶路。」只留「動物王國／語音導覽」。
- 喇叭圖示手機較大（圓 **3.35rem**、圖 **1.6rem**）。
- 主頁不要加「連到獨立子頁」這類結構說明；圖例只做頁內錨點，不要把分區標題做成連結。

---

## 二、整體架構

靜態站，一組資料夾 `~/Project_Animal_Kingdom/`。

| 路徑 | 用途 |
| --- | --- |
| `index.html` + `styles.css` | 主頁（叢林神秘風） |
| `gate.js` | 預覽鎖；主頁 overlay，子頁沒過就回主頁 |
| `guides/*.html` | 二十六則導覽子頁 |
| `guides/tour.css`、`guides/tour-player.js` | 子頁共用博物館播放器 |
| `guides/media/` | 正方形封面、Sal 繁中／英文語音 |
| `media/*-sky.jpg` | 主頁手機分區橫幅 |
| `guides/subpage-spec.md` | 子頁欄位與串接順序 |

兩套視覺**不要混用**：主頁是深夜叢林（極深綠、螢光綠／青／琥珀金、毛玻璃卡片）；子頁是博物館深灰播放器（`#141414`、金強調 `#c9a46a`、直欄最寬 430px）。

---

## 三、主頁設計

### 3.1 風格

- 深林暗影／深夜螢光植物。
- 底：極深叢林綠（約 `#0A190F` / `#07120A`）加夜間煙霧漸層。
- 卡片：半透明毛玻璃。
- 強調：螢光翡翠綠 `#10B981`、螢光青藍 `#06B6D4`、琥珀金 `#F59E0B`。
- 字型：標題 Cormorant Garamond，內文 Noto Sans TC。
- 語氣：園區導覽。不要寫「二十六個類別」「子頁稍後製作」「點名稱就會進入」。

### 3.2 結構（由上到下）

1. **預覽鎖**（未通過不顯示內容）。
2. **導覽列** `.nav-pill`：字標「動物王國」、連結「前言」「分區」。手機約 54px 高、會黏在頂部。
3. **Hero**
   - 主標「動物王國語音導覽」
   - 英文副標 Voice Tour of the Animal Kingdom
   - 導語：戴上耳機、聲音旅程
4. **前言卡**
   - 標籤「行前導覽」（不是「入口電台」）
   - 「1. 前言」連到 `guides/preface.html`
   - 藤蔓 SVG 畫在內文與「開啟前言」**之間**，六個分區色點，不擋標題、不擋按鈕
   - 「開啟前言」波浪動畫按鈕
5. **選擇探索路線**（`#atlas`）
   - 左側圖例只錨到 `#forest` 等頁內 id
   - 右側六張生態區卡
6. **Footer**：只留「動物王國／語音導覽」

### 3.3 六大分區卡（手機直向、桌面網格）

每張卡：頂部橫幅（僅手機照片／桌面漸層）→ 分區名 → 分區導覽列 → 動物列（海洋只有分區導覽）。

點擊目標只有 `.zone-guide`（整列含名稱與喇叭）。不要把 `h3`、圖例、導覽列再做成第二個入口。

| 區塊 | 內容 |
| --- | --- |
| 森林 | 5 森林分區、6 大猩猩、7 大貓熊、8 蒼鷺、9 黑豹、10 棕熊 |
| 草原 | 12 草原分區、13 斑鬣狗、14 大象、15 長頸鹿、16 獅子 |
| 沙漠 | 2 沙漠分區、3 駱駝、4 耳廓狐 |
| 極地 | 22 極地分區、23 海豹、24 麝牛、25 北極熊、26 企鵝 |
| 史前 | 17 史前分區、18 暴龍、19 植食性恐龍、20 迅猛龍、21 翼龍 |
| 海洋 | 11 海洋分區 |

喇叭 `.audio-mini`：手機圓 3.35rem／圖 1.6rem，列高至少 3.5rem；桌面圓 2.35rem／圖 1.25rem。不要縮回 1.85／0.95。

### 3.4 手機橫幅作法（必守）

- 框：高 `4.25rem`，寬約卡片寬。390×844 實測 **356×68**，比例 **5.235:1**。
- 生圖：Grok `21:9`（或 `5:2`），構圖讓地平線在畫面垂直中央，裁扁框後仍看得到天空＋主題物。
- 裁切：`crop_h = round(寬 * 68 / 356)`，再 `Image.crop`。**不要** `resize` 改比例。
- CSS（僅 `max-width: 59.99rem`）：

```css
.zone--grass .sky-grass {
  background: url("media/grass-sky.jpg?v=7") center center / cover no-repeat;
}
```

- 沙漠、海洋卡內仍有 SVG 裝飾；手機已 `display: none`，橫幅用背景圖即可，不必刪 SVG。
- 換圖覆蓋 jpg，並把 `?v=N` 加一。
- 截圖時先避開黏頂導覽列，再拍 `.zone__sky`，否則會拍到「動物王國／前言／分區」。

---

## 四、子頁設計

定版頁：`guides/preface.html`。其餘頁複製它，共用 `tour.css` / `tour-player.js`。**二十六頁都已上線**，不要再當 stub。

### 4.1 視覺

- 背景 `#141414`，字 `#f2f2f2`，金 `#c9a46a`
- 直欄 `max-width: 430px`
- 字型 Noto Sans TC
- **不要**把主頁叢林綠套進來

### 4.2 由上到下（順序固定）

1. 頂欄：`首頁` / `清單` / `導覽`；右上地球＝語言  
   - 首頁／清單／導覽**不做**中英切換，除非另外交待
2. 正方形封面
3. 標題：只有名稱，**禁止** `#001` / `#00N`
4. 播放器：速度、進度條、上一則／倒退 15／播放／快轉 15／下一則
5. Tab：`介紹` / `逐字稿`（預設逐字稿）
6. 內容區

### 4.3 封面

- `guides/media/<slug>-cover.jpg`，檔案 **1024×1024**
- CSS：`aspect-ratio: 1 / 1; object-fit: contain;`　等比縮小、**不裁切**
- 分區封面＝原始棲地，不要路燈、棧道、遊客、標示
- 動物封面＝該棲地裡的動物，不要動物園籠子
- Skywork／artogo 橫幅只是版面參考，不是封面比例

### 4.4 語音

- 男聲 **Sal**（xAI TTS，`voice_id: sal`）
- 繁中 `language: zh` → `<slug>-sal.mp3`
- 英文 `language: en` → `<slug>-sal-en.mp3`
- 切語言時**稿與聲音一起換**，記在 `localStorage ak-guide-lang`
- 地球選單文案：`繁中（繁體中文）` / `English`
- 逐字稿與語音相同；拿掉 `[SH3.1]` 這類製作記號，也不要唸出來
- 英文 HTML 用直引號 `Earth's`

### 4.5 播放器幾何（不要改路徑）

同一水平線：

`上一則(40)` → `倒退15(64)` → **空 28px** → `播放(72，最大)` → **空 28px** → `快轉15(64)` → `下一則(40)`

- ±15：class `skip15`，圖 56×56，點擊 64×64，透明、無框
- 15 是 SVG 文字，不要用 HTML 疊字
- 無上一則／下一則用 disabled `<button>`（前言無上一則，企鵝無下一則）
- 串接順序＝目錄 1→26，**不是**主頁卡片的視覺排法

### 4.6 介紹／逐字稿

- 逐字稿＝附件原文（含他寫的「沈浸」）
- 介紹＝短導覽，不要編造體長、數量、保育等級
- 逐字稿底 `.panel`：`margin: 0; border-radius: 0;` 滿版貼齊左右，文字靠 padding 內縮。這段寫在 `tour.css`，不要每頁複製

### 4.7 每一頁只改這些

`<title>`、導覽 `aria-current`、封面、`h1`、audio 的 zh/en、上一則／下一則、介紹、逐字稿。播放器 HTML（含 ±15 SVG）整段原樣帶走。

---

## 五、二十六則目錄（播放器順序）

| # | 標題 | 檔名 |
| --- | --- | --- |
| 1 | 前言 | `preface.html` |
| 2 | 沙漠生態分區導覽 | `desert.html` |
| 3 | 駱駝 | `camel.html` |
| 4 | 耳廓狐 | `fennec.html` |
| 5 | 森林生態分區導覽 | `forest.html` |
| 6 | 大猩猩 | `gorilla.html` |
| 7 | 大貓熊 | `panda.html` |
| 8 | 蒼鷺 | `heron.html` |
| 9 | 黑豹 | `panther.html` |
| 10 | 棕熊 | `brown-bear.html` |
| 11 | 海洋生態分區導覽 | `ocean.html` |
| 12 | 草原生態分區導覽 | `grassland.html` |
| 13 | 斑鬣狗 | `hyena.html` |
| 14 | 大象 | `elephant.html` |
| 15 | 長頸鹿 | `giraffe.html` |
| 16 | 獅子 | `lion.html` |
| 17 | 史前生命分區導覽 | `prehistoric.html` |
| 18 | 暴龍 | `trex.html` |
| 19 | 植食性恐龍 | `herbivore.html` |
| 20 | 迅猛龍 | `raptor.html` |
| 21 | 翼龍 | `pterosaur.html` |
| 22 | 極地生態分區導覽 | `polar.html` |
| 23 | 海豹 | `seal.html` |
| 24 | 麝牛 | `musk-ox.html` |
| 25 | 北極熊 | `polar-bear.html` |
| 26 | 企鵝 | `penguin.html` |

媒體：`guides/media/<slug>-cover.jpg`、`<slug>-sal.mp3`、`<slug>-sal-en.mp3`。

---

## 六、協作與發布

- 主頁 chrome、分區橫幅、播放器圖示、逐字稿底色：先 `MEDIA:` 截圖，**OK 再推**。
- 其餘視覺／文案：SSH 推 `main`，不要用 GitHub HTTPS PAT。
- 截圖基準：手機 390×844。
- 通行密碼只在 Slack 講，不要寫進 README 或本檔。
- 本機預覽：`python3 -m http.server`；子頁要先在 `sessionStorage` 寫入 `gate.js` 裡的 hash，否則會被導回主頁。
